# Longxia Info Board Agent Runbook

Canonical task spec 已移到：

```text
/home/evan/.openclaw/workspace/tasks/longxia-info-board-daily.md
```

每日 cron 會先執行 usage guard；只有 `allow=true` 時才讀取 canonical task spec。

保留這個檔案只是為了 repo 內可見性與歷史相容，請不要在這裡維護第二份規格。
